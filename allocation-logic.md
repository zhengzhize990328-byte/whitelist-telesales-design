# 白名单电销分配逻辑 v0.1

## 一、目标

分配引擎同时优化五件事：名单新鲜度、客户匹配度、坐席公平性、转化效率和合规边界。它不能只把“最好的客户”永久分给历史转化率最高的坐席，否则会形成强者恒强、无法评估新人能力，也会让团队数据失真。

## 二、名单进入分配池之前

每条名单必须先通过以下硬性检查：

- `whitelist_status = valid`
- 客户未命中禁呼、内部员工、欺诈或重复名单
- 客户当前没有有效归属坐席
- 产品在客户所在国家、地区仍可申请
- 当前时间处于客户允许联系窗口
- 同一手机号、证件号、设备号的重复记录已经合并
- 已申请、已放款或明确拒绝且仍在冷静期的客户不再次进入首呼队列

不通过的名单进入“隔离池”，后台显示拦截原因，不直接删除。

## 三、任务队列顺序

第一期不配置优先级分数、百分比权重或黑盒模型。名单通过硬性检查后进入对应市场与团队队列，按以下固定顺序处理：

1. 到达预约时间的回拨任务返回原坐席。
2. 已接通且处于保护期的客户返回原坐席。
3. 普通新名单按进入合格队列的时间先到先服务。
4. 多次未接、客户要求稍后联系或达到触达上限的名单，按固定保护规则延后或停止。

每条任务需展示命中的规则、进入队列时间和当前保护状态，不展示综合评分。

## 四、路由与坐席选择

### 1. 硬性匹配

先筛出候选坐席：

- 在线且处于可接单状态
- 支持客户语言和产品
- 当前班次允许外呼该地区
- 具备所需合规或产品权限
- 当前任务量未超过上限
- 不处于培训、休息、离线或系统异常状态

### 2. 候选坐席公平轮询

在通过全部硬性匹配的坐席中，先排除已满容量和暂停接单人员，再按以下方式轮转：

- 优先选择当前持有任务量更少的坐席。
- 持有量相同时，选择当日累计分配量更少的坐席。
- 仍相同时，按上一次获配时间轮询。
- 原坐席保护任务不进入普通轮询。

该过程不使用可配置权重，不依据历史转化率把优质名单长期集中给少数坐席。

## 五、客户保护与回收

- 新名单分配后设置短时领取锁；坐席在规定时间内开始处理。
- 已接通且客户表现出意向后，进入原坐席保护期。
- 客户预约回拨默认回到原坐席，并在预约时间到达时进入回拨队列。
- 坐席离线、退出班次、App 长时间未同步、任务超时或主管手动释放时，名单可被回收。
- 回收后保留完整上下文与历史记录；新坐席能看到上次沟通摘要，但敏感字段按权限脱敏。
- 同一客户在任何时刻只能有一个有效主负责人，质检与主管仅拥有查看或协助权限。

## 六、结果状态

App 每次外呼必须选择一个结果：

- 已接通—感兴趣
- 已接通—稍后联系
- 已接通—正在申请
- 已接通—暂不需要
- 未接听
- 忙线
- 号码无效
- 客户要求停止联系
- 其他（必须填写备注）

“稍后联系”必须选择具体时间；“停止联系”立即进入禁呼并阻断后续自动分配。

## 七、主管可配置项

- 各来源名单有效期与进入队列条件
- 各产品、语言、地区与团队的路由关系
- 坐席并发任务数和每日分配上限
- 首呼 SLA、未处理回收时间、意向客户保护期
- 每日允许外呼时段和最大触达次数
- 新人保护配额、实验组比例和保底公平份额
- 手动改派权限与必填原因

## 八、审计与关键指标

每次分配记录：名单 ID、规则版本、命中的准入条件、队列进入时间、候选坐席集合、容量快照、最终坐席、分配时间、回收/改派原因。

核心指标建议包括：

- 名单进入后 5/15/30 分钟首呼率
- 接通率、意向率、申请启动率、申请提交率、放款转化率
- 每坐席分配量、有效通话量和转化率
- 名单回收率、改派率、重复触达率、禁呼误触率
- 不同来源、产品、时段和团队的漏斗差异
- 分配公平度：团队内分配量和高分名单占比的离散程度

## 九、异常降级

- 分配服务不可用：按预生成队列继续发放，禁止重复分配。
- App 弱网：本地缓存当前客户和结果，恢复网络后按操作时间顺序同步。
- 客户状态回传延迟：申请状态以服务端为准，冲突进入人工复核。
- 没有合格坐席：名单进入等待池并通知主管，不跨语言或权限强制分配。
