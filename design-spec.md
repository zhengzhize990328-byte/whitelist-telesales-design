# 白名单电销系统｜设计方向共同规范

## 1. 产品与业务本质

这是一套面向现金贷“白名单客户转化”的跨国电销作业系统，不是催收系统。系统接收已经通过基础准入、风控预筛或营销筛选的潜在借款客户，由安哥拉本地坐席进行外呼，解释贷款产品、确认意向、协助客户完成申请，并持续跟进到提交申请、审批、签约或放款。孟加拉运营团队通过 Web 后台维护名单、团队、分配策略、跟进规则、转化漏斗和质检记录。设计中不得出现逾期、催收、承诺还款、欠款追回等催收语义。

## 2. 用户、语言与使用场景

- App：安哥拉本地电销坐席使用，Android 设备，界面语言为葡萄牙语（pt-AO）。典型场景是移动办公、网络可能不稳定、连续处理大量客户，因此关键动作必须单手可达、信息明确、拨号与结果记录路径短。
- Web：孟加拉运营人员使用，桌面端，主界面语言为孟加拉语（bn-BD），可提供英语切换入口。典型角色包括管理员、运营主管、团队组长和质检人员。
- Web 与 App 是同一业务链路的两端：后台配置规则并分配客户，App 执行触达并回传结果，后台实时查看转化与异常。

## 3. 核心业务流程

名单导入或 API 接入 → 去重与准入校验 → 客户意向/价值评分 → 匹配产品、语言、地区、班次与坐席技能 → 自动分配 → 坐席领取并外呼 → 记录结果 → 创建下次跟进 → 客户申请 → 审批/签约/放款状态回传 → 成交归因或释放回收。

App 代表性页面包括：今日工作台、客户队列、客户详情与推荐话术、通话中辅助、通话结果记录、跟进日程、个人绩效与离线同步状态。方向探索阶段只需展示一个最能体现产品气质的“今日客户详情/下一通”组合页。

Web 代表性页面包括：运营总览、白名单池、分配策略、坐席与团队、转化漏斗、任务监控、质检与审计。方向探索阶段只需展示一个“运营总览 + 分配状态”代表页。

## 4. 分配逻辑

分配引擎采用六层结构：

1. 准入过滤：白名单有效、客户未被禁呼、未重复、处于允许联系时段、产品仍可申请。
2. 路由匹配：国家/地区、语言、产品、营销渠道、坐席技能、班次和合规权限。
3. 优先级评分：名单新鲜度、预估申请概率、预估可贷额度、营销活动优先级、上次触达结果、计划跟进时间。
4. 负载均衡：坐席在线状态、当前未完成任务量、当日上限、平均处理时长、历史转化率；支持加权轮询与手动指定。
5. 客户保护：一个客户同一时间仅归属一个坐席；已接通客户进入保护期；预约回拨优先回到原坐席；坐席离线、超时未处理或超容量时自动回收。
6. 归因与审计：记录规则命中、分配原因、改派原因、坐席操作、客户阶段变化与最终申请/放款归因。

建议默认公式为：优先级分 = 35% 名单新鲜度 + 25% 申请倾向 + 15% 预估价值 + 15% 跟进时效 + 10% 活动权重。坐席选择先做硬性资格匹配，再在候选坐席中使用“容量余量 × 近期接通率校正 × 公平性系数”计算权重。评分权重仅用于原型说明，正式上线前需要业务用历史数据校准。

## 5. 关键状态与文案

潜客阶段：新名单、待首呼、联系中、已接通、感兴趣、预约回拨、申请中、已提交、审批中、已放款、暂不需要、无法联系、号码无效、禁呼。App 葡语示例可使用 “Próximo cliente”“Ligar agora”“Interessado”“Agendar retorno”“Pedido iniciado”。Web 孟加拉语示例可使用 “অপারেশন ওভারভিউ”“হোয়াইটলিস্ট পুল”“বণ্টন নীতি”“আজকের রূপান্তর”“সক্রিয় এজেন্ট”。

## 6. 视觉与交付约束

- 没有现成品牌资产，先探索三套差异明显但都可信赖的方向；不可使用泛化紫色渐变、emoji 图标或无意义数据装饰。
- 三套方向使用同一内容和同一画布尺寸，便于横向比较。每套预览画布为 1600×1000，左侧 Android App 代表屏，右侧桌面 Web 代表屏。
- 视觉温度应是可信、积极、清晰、有行动感；不能像催收系统，也不能像娱乐 App。
- 工具型界面不需要内容图片。图形仅用于真实数据结构、状态或操作反馈。
- Android 设备框基于花叔 Design 的标准 AndroidFrame（Pixel 8 参考规格），不自行设计刘海、状态栏或导航栏。
- 方向 A：运营秩序感强、明亮、高密度、适合规模化团队。
- 方向 B：更亲和、更强调坐席节奏与客户关系，使用温暖底色与柔和对比。
- 方向 C：更强的实时调度与控制塔气质，高对比但不使用通用霓虹科技风。

## 7. 方向探索阶段的成功标准

用户应能在 10 秒内看出：这是白名单销售转化系统；App 负责“下一位客户与快速外呼”；Web 负责“名单分配、坐席状态与转化监控”；两端属于同一产品。三套方向必须是结构和气质上的真实差异，不是只换颜色。最终推荐应优先考虑跨文化可读性、长时间操作舒适度、手机弱网环境和后台高密度监控效率。
